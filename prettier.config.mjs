/** @type {import('prettier').Config} */
const config = {
  arrowParens: "always",
  printWidth: 90,
  singleQuote: false,
  semi: true,
  tabWidth: 2,
  trailingComma: "all",
};

export default config;
